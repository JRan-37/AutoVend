# deps & build output
node_modules/
dist/
build/
.react-router/
coverage/
*.tsbuildinfo
pnpm-debug.log*
*.sarif

# test artifacts
test-results/
playwright-report/

# env & secrets — this repo is public; nothing sensitive is ever committed
.env
.env.*
!.env.example

# terraform (state lives in S3; account-specific values stay local;
# .terraform.lock.hcl IS committed — provider supply-chain pinning)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
backend.hcl

# OS noise
.DS_Store
Thumbs.db
